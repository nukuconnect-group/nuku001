# THIS FILE HAS BEEN SANITIZED — secrets removed.
# Do NOT store secrets in the repository. Use your deployment provider (Vercel, Netlify, Render, Supabase, etc.)
# or a local .env.local file which is excluded by .gitignore.

# Example placeholders (empty):
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# If you need service_role/service keys, keep them *only* in server-side environments (Supabase dashboard, CI secrets, etc.)
